# client/config.py
SERVER_PORT = 9999
# Default to localhost for easy local testing. Change to server LAN IP when testing across machines.
SERVER_HOST = "127.0.0.1"
# Configure the server IP and port (set to the server machine's LAN IP)

