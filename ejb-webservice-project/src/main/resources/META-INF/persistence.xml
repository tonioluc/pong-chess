<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.2"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">
    
    <persistence-unit name="viePU" transaction-type="JTA">
        <jta-data-source>java:jboss/datasources/VieDS</jta-data-source>
        
        <!-- Entités JPA -->
        <class>com.vie.entity.Vie</class>
        
        <properties>
            <!-- Configuration Hibernate -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            
            <!-- 
            Stratégie de génération du schéma:
            - create: Supprime et recrée les tables à chaque démarrage (développement)
            - update: Met à jour le schéma sans supprimer les données (développement)
            - validate: Valide le schéma sans le modifier (production)
            - none: Ne fait rien (production avec scripts SQL manuels)
            
            COMMENTEZ cette ligne en production pour éviter la réinitialisation automatique
            -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            
            <!-- Cache de second niveau (optionnel) -->
            <property name="hibernate.cache.use_second_level_cache" value="false"/>
            <property name="hibernate.cache.use_query_cache" value="false"/>
        </properties>
    </persistence-unit>
</persistence>
